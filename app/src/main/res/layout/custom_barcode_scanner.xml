<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- 相机预览 -->
    <com.journeyapps.barcodescanner.BarcodeView
        android:id="@+id/zxing_barcode_surface"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 自定义遮罩视图（颜色 token 见 values/colors.xml scanner_*，扫码页恒暗、不随主题切换） -->
    <com.x.client.app.ScannerOverlayView
        android:id="@+id/zxing_viewfinder_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:scannerMaskColor="@color/scanner_mask"
        app:scannerFrameColor="@color/scanner_frame"
        app:scannerCornerColor="@color/scanner_corner"
        app:scannerLineColor="@color/scanner_line" />

    <!-- 提示文本 -->
    <TextView
        android:id="@+id/zxing_status_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="80dp"
        android:text="@string/scanner_hint"
        android:textColor="@color/scanner_frame"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        android:background="@color/scanner_mask"
        android:padding="@dimen/spacing_md" />

</merge>
