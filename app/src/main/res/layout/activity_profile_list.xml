<?xml version="1.0" encoding="utf-8"?>
<!--
    主页（连接页）精修（refine-ux-review §4）：顶部去 CTA 化，页面唯一主操作 = 左下角 VPN FAB。
    - Toolbar：仅应用名 + 版本号（被动信息）
    - 配置列表：当前项 primaryContainer 高亮即"当前连接上下文"
    - 左下 VPN FAB（五态 morph）+ 右下 Add FAB：尺寸/margin/elevation 对称
    - 空态：EmptyStateView 指向 Add FAB
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        style="@style/Widget.XClient.Toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/profile_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingBottom="88dp"
            android:clipToPadding="false" />

        <com.x.client.app.ui.EmptyStateView
            android:id="@+id/empty_state"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />

        <!-- 左下：VPN 连接主操作（compact 56dp，morph 展开为状态卡） -->
        <com.x.client.app.ui.ConnectionFab
            android:id="@+id/vpn_fab"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="bottom|start"
            android:layout_marginStart="@dimen/spacing_lg"
            android:layout_marginBottom="@dimen/spacing_xl" />

        <!-- 右下：添加/导入（次级操作，与 VPN FAB 对称） -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_main"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="@dimen/spacing_lg"
            android:layout_marginBottom="@dimen/spacing_xl"
            android:contentDescription="@string/acc_add_profile"
            android:src="@drawable/ic_add"
            app:tint="?attr/colorOnPrimaryContainer"
            app:backgroundTint="?attr/colorPrimaryContainer" />

    </FrameLayout>

</LinearLayout>
